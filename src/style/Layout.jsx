import React from "react";

function Layout() {
  const styled = {
    background: "azure",
    width: "calc(30% - 10px)",
    marginLeft: "10px",
  };
  return <div style={styled}>Layout</div>;
}

export default Layout;
